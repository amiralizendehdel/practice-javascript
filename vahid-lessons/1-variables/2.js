const myObject = { vahid: 12 };

// myObject = 13; // it's not ok
myObject.vahid = 13; // it's ok

// =================

const asdsd = 123123;
const calculator = {
  add: function () {},
  minus: function () {},
};

// ============================

const game = {
  play: function () {},
  add: function () {},
};

calculator.add(sdfsdf);
game.add();
